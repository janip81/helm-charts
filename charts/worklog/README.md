# worklog

![Version: 0.1.1](https://img.shields.io/badge/Version-0.1.1-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 0.1.0](https://img.shields.io/badge/AppVersion-0.1.0-informational?style=flat-square)

Worklog salary tracker — Go API + React frontend

## Maintainers

| Name | Email | Url |
| ---- | ------ | --- |
| janip81 | <jani@techmonkeys.se> | <https://github.com/janip81> |

# Overview

Helm chart for the Worklog salary tracker — Go API + React frontend.

## Adding this helm repository

```bash
helm repo add janip81 https://janip81.github.io/helm-charts/
helm search repo worklog
```

## TL;DR

```bash
helm repo add janip81 https://janip81.github.io/helm-charts/
helm install worklog janip81/worklog -f your-values.yaml
```

## Prerequisites

- Kubernetes with Gateway API (Cilium)
- CNPG `Database` CRD applied to create the `worklog` db on `pg-main`
- A Secret named `worklog-api-secret` with keys: `DATABASE_URL`, `KEYCLOAK_URL`, `KEYCLOAK_REALM`, `FRONTEND_ORIGIN`

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| api.image.pullPolicy | string | `"IfNotPresent"` | Image pull policy |
| api.image.repo | string | `"ghcr.io/janip81/worklog-api"` | API container image repository |
| api.image.tag | string | `"latest"` | API image tag |
| api.resources.limits.cpu | string | `"200m"` | CPU limit for the API container |
| api.resources.limits.memory | string | `"128Mi"` | Memory limit for the API container |
| api.resources.requests.cpu | string | `"50m"` | CPU request for the API container |
| api.resources.requests.memory | string | `"64Mi"` | Memory request for the API container |
| api.secretName | string | `"worklog-api-secret"` | Name of the Secret containing DATABASE_URL, KEYCLOAK_URL, KEYCLOAK_REALM |
| api.service.port | int | `8081` | Service port for the API |
| frontend.image.pullPolicy | string | `"IfNotPresent"` | Image pull policy |
| frontend.image.repo | string | `"ghcr.io/janip81/worklog-frontend"` | Frontend container image repository |
| frontend.image.tag | string | `"latest"` | Frontend image tag |
| frontend.resources.limits.cpu | string | `"100m"` | CPU limit for the frontend container |
| frontend.resources.limits.memory | string | `"64Mi"` | Memory limit for the frontend container |
| frontend.resources.requests.cpu | string | `"20m"` | CPU request for the frontend container |
| frontend.resources.requests.memory | string | `"32Mi"` | Memory request for the frontend container |
| frontend.service.port | int | `80` | Service port for the frontend |
| gateway | object | `{"apiHostname":"worklog-api.prod.threshold.se","frontendHostname":"worklog.prod.threshold.se","name":"internal-shared","namespace":"kube-system"}` | Gateway API HTTPRoute settings (Cilium / internal-shared gateway) |
| gateway.apiHostname | string | `"worklog-api.prod.threshold.se"` | Hostname for the Go API |
| gateway.frontendHostname | string | `"worklog.prod.threshold.se"` | Hostname for the React frontend |
| gateway.name | string | `"internal-shared"` | Gateway name |
| gateway.namespace | string | `"kube-system"` | Gateway namespace |
| imagePullSecret | string | `""` | Image pull secret name for GHCR (kubernetes.io/dockerconfigjson) |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
