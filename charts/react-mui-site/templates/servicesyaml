apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-react-site.fullname" . }}
spec:
  selector:
    app: {{ include "my-react-site.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
