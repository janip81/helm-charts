{{- if .Values.cnpg.enabled }}
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: {{ include "remnant.fullname" . }}-pg
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "remnant.labels" . | nindent 4 }}
spec:
  instances: {{ .Values.cnpg.instances }}
  imageName: ghcr.io/cloudnative-pg/postgresql:16

  storage:
    size: {{ .Values.cnpg.storage.size }}
    storageClass: {{ .Values.cnpg.storage.storageClass }}

  postgresql:
    shared_preload_libraries:
      - vector

  bootstrap:
    initdb:
      database: {{ .Values.cnpg.database | default "remnant" }}
      owner: {{ .Values.cnpg.owner | default "remnant" }}
      postInitSQL:
        - "CREATE EXTENSION IF NOT EXISTS vector;"

  {{- if .Values.cnpg.backup.enabled }}
  backup:
    barmanObjectStore:
      destinationPath: {{ .Values.cnpg.backup.destinationPath | quote }}
      endpointURL: {{ .Values.cnpg.backup.endpointURL | quote }}
      s3Credentials:
        accessKeyId:
          name: {{ .Values.cnpg.backup.s3CredentialsSecret }}
          key: access-key-id
        secretAccessKey:
          name: {{ .Values.cnpg.backup.s3CredentialsSecret }}
          key: secret-access-key
        region:
          name: {{ .Values.cnpg.backup.s3CredentialsSecret }}
          key: region
      wal:
        compression: gzip
      data:
        compression: gzip
    retentionPolicy: {{ .Values.cnpg.backup.retentionPolicy | quote }}
  {{- end }}
{{- end }}
