{{- if and .Values.exporter.enabled .Values.exporter.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "zomboid-server.fullname" . }}-exporter
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zomboid-server.labels" . | nindent 4 }}
  {{- with .Values.exporter.gateway.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- toYaml .Values.exporter.gateway.parentRefs | nindent 4 }}
  hostnames:
    - {{ .Values.exporter.gateway.hostname | quote }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "zomboid-server.fullname" . }}-metrics
          port: {{ .Values.exporter.port }}
{{- end }}
