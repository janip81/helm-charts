castlist has been deployed to namespace: {{ .Release.Namespace }}

Access the UI at: http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}

Make sure:
1. Your Keycloak client redirect URI includes: http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}/*
2. CHROMECAST_IP is reachable from the API pod (same LAN or routed)

To check status:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl logs -n {{ .Release.Namespace }} deploy/castlist-api
