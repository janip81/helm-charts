apiVersion: v1
kind: Service
metadata:
  name: castlist-api
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: castlist-api
  ports:
    - port: {{ .Values.api.port }}
      targetPort: {{ .Values.api.port }}
  type: ClusterIP
