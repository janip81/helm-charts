apiVersion: v1
kind: Service
metadata:
  name: castlist-frontend
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: castlist-frontend
  ports:
    - port: 80
      targetPort: {{ .Values.frontend.port }}
  type: ClusterIP
