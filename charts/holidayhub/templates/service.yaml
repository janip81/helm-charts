apiVersion: v1
kind: Service
metadata:
  name: {{ include "holidayhub.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "holidayhub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "holidayhub.selectorLabels" . | nindent 4 }}
